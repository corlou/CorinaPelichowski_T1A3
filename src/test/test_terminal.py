from terminal import TerminalApp
from deck import tarot_cards


# Test Setup
FILE_PATH = "./src/deck.py"
terminal_app = TerminalApp(tarot_cards)
terminal_app.run()

# When a user gets a reading, make sure that reading is added to 'get history' = Test


# def reading_added_to_history():
# CHECK THAT HISTORY IS 0
